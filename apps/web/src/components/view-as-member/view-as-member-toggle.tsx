import { ViewAsSelector } from './view-as-selector';

/**
 * Toggle component for super admins - now uses the ViewAsSelector component
 * @deprecated Use ViewAsSelector directly instead
 */
export const ViewAsMemberToggle = ViewAsSelector;
